node_modules/
.next/
out/
build/
dist/
coverage/
test-results/
playwright-report/
.tsbuildinfo
*.tsbuildinfo
next-env.d.ts

# Secrets — never commit
.env
.env.local
.env.*.local

# Local development storage (uploaded scripts, audio, stills, renders).
# Anchored with a leading slash: an unanchored "storage/" would also match
# src/lib/storage/ and silently drop the storage driver from the repo.
/storage/*
!/storage/.gitkeep

.DS_Store
Thumbs.db
